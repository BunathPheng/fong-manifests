---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fong                  # Ingress name
  labels:
    app: fong
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "fong.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fong               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
